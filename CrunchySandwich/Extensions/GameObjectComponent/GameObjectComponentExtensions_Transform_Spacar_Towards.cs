using System;
using System.Collections;
using System.Collections.Generic;

using UnityEngine;

namespace Crunchy.Sandwich
{
    using Dough;    
    static public class GameObjectComponentExtensions_Transform_Spacar_Towards
    {
		static public void TowardsSpacarPosition(this GameObject item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsSpacarPosition(target, amount);
        }
        static public void TowardsLocalSpacarPosition(this GameObject item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsLocalSpacarPosition(target, amount);
        }
        
        static public void TowardsSpacarPosition(this GameObject item, Vector3 target, float amount)
        {
            item.transform.TowardsSpacarPosition(target, amount);
        }
        static public void TowardsLocalSpacarPosition(this GameObject item, Vector3 target, float amount)
        {
            item.transform.TowardsLocalSpacarPosition(target, amount);
        }

        static public void TowardsSpacarScale(this GameObject item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsSpacarScale(target, amount);
        }
        static public void TowardsLocalSpacarScale(this GameObject item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsLocalSpacarScale(target, amount);
        }

		static public void TowardsSpacarScale(this GameObject item, float target, float amount)
        {
            item.transform.TowardsSpacarScale(target, amount);
        }
        static public void TowardsLocalSpacarScale(this GameObject item, float target, float amount)
        {
            item.transform.TowardsLocalSpacarScale(target, amount);
        }

		static public void TowardsSpacarPosition(this Component item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsSpacarPosition(target, amount);
        }
        static public void TowardsLocalSpacarPosition(this Component item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsLocalSpacarPosition(target, amount);
        }
        
        static public void TowardsSpacarPosition(this Component item, Vector3 target, float amount)
        {
            item.transform.TowardsSpacarPosition(target, amount);
        }
        static public void TowardsLocalSpacarPosition(this Component item, Vector3 target, float amount)
        {
            item.transform.TowardsLocalSpacarPosition(target, amount);
        }

        static public void TowardsSpacarScale(this Component item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsSpacarScale(target, amount);
        }
        static public void TowardsLocalSpacarScale(this Component item, Vector3 target, Vector3 amount)
        {
            item.transform.TowardsLocalSpacarScale(target, amount);
        }

		static public void TowardsSpacarScale(this Component item, float target, float amount)
        {
            item.transform.TowardsSpacarScale(target, amount);
        }
        static public void TowardsLocalSpacarScale(this Component item, float target, float amount)
        {
            item.transform.TowardsLocalSpacarScale(target, amount);
        }

	}
}