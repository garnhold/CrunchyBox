using System;
using System.Collections;
using System.Collections.Generic;

using UnityEngine;

using CrunchyDough;
using CrunchyBun;

namespace CrunchySandwich
{
	static public class GameObjectComponentExtensions_Transform_Planar_Towards
    {
		static public void TowardsPlanarPosition(this GameObject item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsPlanarPosition(target, amount);
        }
        static public void TowardsLocalPlanarPosition(this GameObject item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsLocalPlanarPosition(target, amount);
        }
        
        static public void TowardsPlanarPosition(this GameObject item, Vector2 target, float amount)
        {
            item.transform.TowardsPlanarPosition(target, amount);
        }
        static public void TowardsLocalPlanarPosition(this GameObject item, Vector2 target, float amount)
        {
            item.transform.TowardsLocalPlanarPosition(target, amount);
        }

        static public void TowardsPlanarRotation(this GameObject item, float target, float amount)
        {
            item.transform.TowardsPlanarRotation(target, amount);
        }
        static public void TowardsLocalPlanarRotation(this GameObject item, float target, float amount)
        {
            item.transform.TowardsLocalPlanarRotation(target, amount);
        }

        static public void TowardsPlanarScale(this GameObject item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsPlanarScale(target, amount);
        }
        static public void TowardsLocalPlanarScale(this GameObject item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsLocalPlanarScale(target, amount);
        }

		static public void TowardsPlanarScale(this GameObject item, float target, float amount)
        {
            item.transform.TowardsPlanarScale(target, amount);
        }
        static public void TowardsLocalPlanarScale(this GameObject item, float target, float amount)
        {
            item.transform.TowardsLocalPlanarScale(target, amount);
        }

		static public void TowardsPlanarPosition(this Component item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsPlanarPosition(target, amount);
        }
        static public void TowardsLocalPlanarPosition(this Component item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsLocalPlanarPosition(target, amount);
        }
        
        static public void TowardsPlanarPosition(this Component item, Vector2 target, float amount)
        {
            item.transform.TowardsPlanarPosition(target, amount);
        }
        static public void TowardsLocalPlanarPosition(this Component item, Vector2 target, float amount)
        {
            item.transform.TowardsLocalPlanarPosition(target, amount);
        }

        static public void TowardsPlanarRotation(this Component item, float target, float amount)
        {
            item.transform.TowardsPlanarRotation(target, amount);
        }
        static public void TowardsLocalPlanarRotation(this Component item, float target, float amount)
        {
            item.transform.TowardsLocalPlanarRotation(target, amount);
        }

        static public void TowardsPlanarScale(this Component item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsPlanarScale(target, amount);
        }
        static public void TowardsLocalPlanarScale(this Component item, Vector2 target, Vector2 amount)
        {
            item.transform.TowardsLocalPlanarScale(target, amount);
        }

		static public void TowardsPlanarScale(this Component item, float target, float amount)
        {
            item.transform.TowardsPlanarScale(target, amount);
        }
        static public void TowardsLocalPlanarScale(this Component item, float target, float amount)
        {
            item.transform.TowardsLocalPlanarScale(target, amount);
        }

	}
}